/*The addList() function adds a new li element to the ul element with id 'list'*/function addList()	{		ul = document.getElementById('list');		newLi = document.createElement('li'); // Create a new li element		newTextNode = document.createTextNode(document.getElementsByName('nameOfItem')[0].value); // Create a new text node with it's text obtained from the text field		document.getElementsByName('nameOfItem')[0].value = ''; // Clear the text field		newLi.appendChild(newTextNode); // Append text node to li.		ul.appendChild(newLi); // Append li to ul.	}