/*The addList() function adds a new li element to the ul element with id 'list'*/function addList()	{		switch(validateTextBox())		{		case 1:		  	alert('Text cannot exceed 200 characters.')			document.getElementsByName('toDo')[0].focus();			return;		case 2:		  	alert('Enter some text.')			document.getElementsByName('toDo')[0].focus();			return;		  break;		default:break;		   		}		ul = document.getElementById('list');		newLi = document.createElement('li'); // Create a new li element		newSpan = document.createElement('span'); // Create a new span element to enclose checkbox and text.		newTextNode = document.createTextNode(document.getElementsByName('toDo')[0].value); // Create a new text node with it's text obtained from the text field		document.getElementsByName('toDo')[0].value = ''; // Clear the text field		newSpan.appendChild(newTextNode); // Append text node to span.		newCheckbox = document.createElement('input'); // Create a new input.		newCheckbox.type = 'checkbox' ;		newCheckbox.value = 'check' ;				newLi.appendChild(newCheckbox); //Append checkbox to span		newLi.appendChild(newSpan); // Append span to li.		newLi.className = 'li_' + (document.getElementById('list').childElementCount % 2)		ul.appendChild(newLi); // Append li to ul.		updateCount();		document.getElementById('currentTime').innerHTML = "Last Added : " + getCurrentDate();	}function validateTextBox()	{		textValue = document.getElementsByName('toDo')[0].value;				if (textValue.length >200)		{			return 1;		}				if (textValue.length === 0)		{			return 2;		}	}	function updateCount()	{		countContainer = document.getElementById('listCount');		countContainer.innerHTML = "Todos(" + document.getElementById('list').childElementCount + ")";	}	function getCurrentDate()	{		var a_p = "";		var date = new Date();		var currentHour = date.getHours();		a_p = currentHour < 12 ? "AM" : "PM"		if (currentHour == 0)		   {		   currentHour = 12;		   }		if (currentHour > 12)		   {		   currentHour = currentHour - 12;		   }		var currentMinute = date.getMinutes()+ "";		if(currentMinute.length == 1)		{			currentMinute = "0" + currentMinute;		}		return(currentHour + " : " + currentMinute + " " + a_p);	}